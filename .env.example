# Not used yet — the app currently runs entirely against local seeded
# SQLite data (see README "Wiring in real Supabase/Stripe data"). These are
# placeholders for when the data-source layer is swapped over.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
